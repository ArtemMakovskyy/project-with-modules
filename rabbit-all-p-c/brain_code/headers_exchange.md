# 1️⃣ Что такое Headers Exchange (коротко)

- **routingKey игнорируется**
    
- маршрутизация идёт **по headers**
    
- биндинг описывается как:
    
    - `whereAll(...)` → AND
        
    - `whereAny(...)` → OR
        

📌 Используется, когда routing key неудобен:

- версия API
    
- тип клиента
    
- роль
    
- feature flags